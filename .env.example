# Mock legacy portal
PORT=3000
MOCK_BASE_URL=http://127.0.0.1:3000

# Discovery LLM only — never read by src/engine (Replay)
# Copy this file to .env, then paste your Anthropic API key after the = (no quotes)
ANTHROPIC_API_KEY=
DISCOVERY_MODEL=claude-sonnet-4-6
